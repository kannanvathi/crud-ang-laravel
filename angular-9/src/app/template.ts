export class template {
    id: any;
    name: string;
    email: any;
    password:any;

  constructor(name, email, password){
    this.id = 0,
    this.name = name,
    this.email = email,
    this.password = password
   }
}